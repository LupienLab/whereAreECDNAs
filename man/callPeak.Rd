% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CallPeak.R
\name{callPeak}
\alias{callPeak}
\title{Perform peak calling with MACS2}
\usage{
callPeak(
  object = NULL,
  macs2Path = NULL,
  outputDir = NULL,
  format = "BED",
  genome = NULL,
  shift = -100L,
  extsize = 200L,
  qval = 0.05,
  nThreads = 1,
  extraArgs = NULL,
  keepStandard = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{object}{A \code{whereAreECDNAs} object.}

\item{macs2Path}{Character(1). Path to \code{macs2} executable.}

\item{outputDir}{Character(1). Base output directory. If \code{NULL}, uses the
\code{outputDirectory} stored in \code{object}, or \code{paste0(sampleName, ".outputs")}
when no stored directory is available.}

\item{format}{Character(1). MACS2 input format. The current implementation
writes three-column BED input and therefore supports \code{"BED"} (default).}

\item{genome}{Character(1), numeric(1), or \code{NULL}. MACS2 \code{-g} argument. If
\code{NULL}, the value is inferred from \code{object@genomeAnnotation$genome} when
possible.}

\item{shift}{Integer(1). MACS2 \code{--shift}.}

\item{extsize}{Integer(1). MACS2 \code{--extsize}.}

\item{qval}{Numeric(1). MACS2 \code{-q} in (0, 1]. Default is 0.05.}

\item{nThreads}{Integer(1) >= 1. Threads for \code{data.table::fwrite()} (default: 1).}

\item{extraArgs}{Character vector of additional MACS2 arguments (default: \code{NULL}).}

\item{keepStandard}{Logical(1). Whether to retain only standard chromosomes.}

\item{verbose}{Logical(1). Whether to print progress messages.}
}
\value{
The input object with narrow peak information stored in \code{object@narrowPeak}.
}
\description{
Performs peak calling with MACS2 on a three-column BED file derived from
pseudobulk fragments. The resulting \code{.narrowPeak} file is imported as a
\code{GRanges} object and stored in the \code{narrowPeak} slot.
}
\details{
The default MACS2 settings are chosen for pseudobulk single-cell ATAC-seq
fragments represented as BED-like intervals. \code{--nomodel}, \code{--shift -100},
and \verb{--extsize 200} are commonly used to center and extend Tn5 insertion
signal when calling narrow ATAC-seq peaks from BED input. For more stringent
peak sets, consider reducing \code{qval} from \code{0.05} to \code{0.01}.
}
\examples{
\dontrun{
  object <- callPeak(
    object = object,
    macs2Path = "/path/to/macs2",
    nThreads = 10
  )
}

}
