% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/QuantifyECDNAContent.R
\name{quantifyECDNAContent}
\alias{quantifyECDNAContent}
\title{Quantify ecDNA content using the Mann-Whitney U statistic}
\usage{
quantifyECDNAContent(object = NULL, tiesMethod = "min", verbose = TRUE)
}
\arguments{
\item{object}{A \code{whereAreECDNAs} object containing ecDNA amplicons,
a cell-by-peak accessibility matrix, and cell metadata.}

\item{tiesMethod}{Character(1). Ties handling passed to \code{base::rank()} when
ranking accessibility values. Options include \code{"average"}, \code{"first"}, \code{"last"},
\code{"random"}, \code{"max"}, and \code{"min"}. Default is \code{"min"}.}

\item{verbose}{Logical(1). Whether to print progress messages (default: \code{TRUE}).}
}
\value{
A \code{whereAreECDNAs} object with ecDNA content scores added to
\code{object@cellColData$content}. The ecDNA-restricted cell-by-peak matrix is
stored in \code{object@subcellPeakMatrix}.
}
\description{
Quantifies cell-level ecDNA content using an ecDNA-restricted cell-by-peak
accessibility matrix. Peaks overlapping ecDNA amplicons are first selected,
and each cell is assigned an ecDNA content score based on a
Mann-Whitney U statistic.
}
\examples{
\dontrun{
  object <- quantifyECDNAContent(
    object = object,
    tiesMethod = "min"
  )
}
}
