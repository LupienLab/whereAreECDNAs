% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PredictECDNACells.R
\name{predictECDNACells}
\alias{predictECDNACells}
\title{Predict ecDNA-positive cells using TabPFN}
\usage{
predictECDNACells(
  object = NULL,
  tabpfn = NULL,
  numpy = NULL,
  torch = NULL,
  manualForce = FALSE,
  proportion = c(0.05, 0.05),
  batchSize = 500,
  cpuForce = FALSE,
  proThreshold = 0.5,
  seed = 17,
  verbose = TRUE
)
}
\arguments{
\item{object}{A \code{whereAreECDNAs} object.}

\item{tabpfn}{Python module for \code{tabpfn}, imported through \code{reticulate}.}

\item{numpy}{Python module for \code{numpy}, imported through \code{reticulate}.}

\item{torch}{Python module for \code{torch}, imported through \code{reticulate}.}

\item{manualForce}{Logical(1). Whether to use manually defined training thresholds
(default: \code{FALSE}).}

\item{proportion}{Numeric vector of length 2. The lower and upper tail
proportions used to define putative negative and positive training cells
when quantile-based thresholds are used. Default is \code{c(0.1, 0.1)}.}

\item{batchSize}{Integer(1). Number of cells processed per inference batch (default: \code{500}).}

\item{cpuForce}{Logical(1). Whether to force TabPFN to run on CPU (default: \code{FALSE}).}

\item{proThreshold}{Numeric(1). Probability threshold used to classify cells as ecDNA-positive (default: \code{0.5}).}

\item{seed}{Integer(1). Random seed passed to TabPFN (default: \code{1}).}

\item{verbose}{Logical(1). Whether to print progress messages (default: \code{TRUE}).}
}
\value{
An updated \code{whereAreECDNAs} object with two additional columns in
\code{object@cellColData}: \code{probability} and \code{status}.
}
\description{
Predicts ecDNA-positive and ecDNA-negative cells from an ecDNA-restricted
cell-by-peak accessibility matrix using a TabPFN classifier. Putative
ecDNA-positive and ecDNA-negative training cells are first defined based on
density-derived ecDNA content thresholds. The trained classifier is then used
to estimate ecDNA-positivity probabilities for all cells.
}
\details{
Putative training labels are derived from the distribution of cell-level
ecDNA content. The density is estimated with a modest bandwidth increase to
stabilize summit detection, and negligible tail maxima are excluded. When
two supported modes are detected, the function identifies the low-content
and high-content summits and the intervening valley. Cells at or below the
low-content summit are treated as putative ecDNA-negative training cells,
and cells above the high-content summit are treated as putative ecDNA-positive
training cells. This provides conservative training labels while leaving the
intermediate region unlabelled. If a supported bimodal structure is not
detected, or if \code{manualForce = TRUE}, the function uses the lower and upper
quantiles specified by \code{proportion}.
}
\examples{
\dontrun{
  object <- predictECDNACells(
    object = object,
    tabpfn = tabpfn,
    numpy = numpy,
    torch = torch
  )
}
}
