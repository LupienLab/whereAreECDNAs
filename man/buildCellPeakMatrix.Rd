% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BuildCellPeakMatrix.R
\name{buildCellPeakMatrix}
\alias{buildCellPeakMatrix}
\title{Build a weighted cell-by-peak accessibility matrix}
\usage{
buildCellPeakMatrix(
  object = NULL,
  tileSize = 500,
  peakScaleFactor = 5,
  scaleTo = 10000,
  ceiling = 8,
  excludeChr = c("chrM"),
  blacklist = NULL,
  tmpFile = NULL,
  nThreads = 1,
  verbose = TRUE
)
}
\arguments{
\item{object}{A \code{whereAreECDNAs} object containing \code{fragment}, \code{narrowPeak},
and cell barcodes accessible as \code{rownames(object@cellColData)}.}

\item{tileSize}{Integer(1). Tile size, in base pairs, for discretizing fragment positions (default: \code{500}).}

\item{peakScaleFactor}{Numeric(1). Scaling factor for peak-width-based weighting (default: \code{5}).}

\item{scaleTo}{Numeric(1). Target scale factor applied after per-cell
library-size normalization by total weighted peak accessibility
(default: \code{10000}).}

\item{ceiling}{Numeric(1). Maximum value for count clipping (default: \code{8}).}

\item{excludeChr}{Character vector of chromosome names to exclude (default: \code{c("chrM")}).}

\item{blacklist}{Optional \code{GRanges} object containing blacklist regions to exclude.}

\item{tmpFile}{Character(1). Prefix/path for temporary \code{.rds} files (default: \code{NULL}, auto-generated).}

\item{nThreads}{Integer(1) >= 1. Number of threads (default: \code{1}).}

\item{verbose}{Logical(1). Whether to print progress messages.}
}
\value{
The input \code{whereAreECDNAs} object with \code{object@cellPeakMatrix}
populated as a sparse cells-by-peaks matrix.
}
\description{
Builds a weighted cell-by-peak accessibility matrix from single-cell
ATAC-seq fragments and peak regions. Fragment start/end positions are
discretized into genomic tiles, cell-by-tile coverage is computed, and
cell-by-peak accessibility is calculated using peak-specific weights.
}
\details{
The matrix is constructed in four steps. First, fragment start and end
positions are binned into fixed-width genomic tiles to represent local
insertion signal for each cell. Second, tiles overlapping each peak are
linked to that peak through a tile-by-peak mapping matrix and assigned a
peak-level weight. The default weighting scheme gives narrower peaks higher
weights, because focal accessibility events are more likely to represent
specific regulatory signal than broad regions. Weights are linearly scaled
from \code{1} for the widest peak to \code{peakScaleFactor} for the narrowest peak.
Third, blacklist regions are removed from the tile-by-peak mapping when
available. Finally, chromosome
matrices are merged and normalized within each cell by the total weighted
accessibility signal across candidate peaks, followed by scaling to
\code{scaleTo}. This produces a sparse cells-by-peaks accessibility matrix
suitable for downstream ecDNA content quantification.
}
\examples{
\dontrun{
  object <- buildCellPeakMatrix(
    object = object,
    nThreads = 10
  )
}
}
