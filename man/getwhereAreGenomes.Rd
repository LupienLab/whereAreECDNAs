% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LoadGenome.R
\name{getwhereAreGenomes}
\alias{getwhereAreGenomes}
\title{Get the default genome or its annotation}
\usage{
getwhereAreGenomes(genomeAnnotation = FALSE)
}
\arguments{
\item{genomeAnnotation}{Logical(1). If \code{FALSE} (default), return the genome
label as a string (e.g., \code{"Hg38"}). If \code{TRUE}, loads and returns the
corresponding genome annotation object.}
}
\value{
\itemize{
\item If \code{genomeAnnotation = FALSE}: a character scalar with the genome label
(e.g. \code{"Hg38"}), or \code{NULL} if no default genome is set.
\item If \code{genomeAnnotation = TRUE}: the genome annotation object stored in
\verb{genomeAnno*}, or \code{NULL} if no default genome is set.
}
}
\description{
This helper reads the global option set by \code{\link[=addwhereAreGenomes]{addwhereAreGenomes()}} and returns
either the genome label (e.g., \code{"Hg38"}) or the corresponding genome
annotation object stored in package data (e.g., \code{genomeAnnoHg38}).
}
\examples{
\dontrun{
  # Return the genome label
  getwhereAreGenomes()

  # Return the annotation object
  anno <- getwhereAreGenomes(genomeAnnotation = TRUE)
}

}
