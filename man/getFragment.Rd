% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/InputData.R
\name{getFragment}
\alias{getFragment}
\title{Read fragment intervals}
\usage{
getFragment(fragmentPath, nThreads = 1, keepStandard = TRUE, verbose = TRUE)
}
\arguments{
\item{fragmentPath}{Character(1). Path to a fragment file (\code{.tsv} or \code{.tsv.gz}).
The expected columns are chromosome, start, end, barcode, and optional count.}

\item{nThreads}{Integer(1). Number of threads for \code{data.table::fread()} (default: \code{1}).}

\item{keepStandard}{Logical(1). Whether to retain only standard chromosomes.}

\item{verbose}{Logical(1). Whether to print progress messages.}
}
\value{
A \code{GRanges} object containing fragment intervals, with \code{barcode} and
\code{count} metadata columns.
}
\description{
Reads a single-cell ATAC-seq fragment file and returns a \code{GRanges} object.
}
\examples{
\dontrun{
  fragment <- getFragment(
    fragmentPath = "/path/to/fragment",
    nThreads = 1
  )
  class(fragment)  # "GRanges"
}

}
