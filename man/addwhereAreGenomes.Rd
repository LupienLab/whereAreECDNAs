% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LoadGenome.R
\name{addwhereAreGenomes}
\alias{addwhereAreGenomes}
\title{Set or query the default genome build}
\usage{
addwhereAreGenomes(genome = NULL)
}
\arguments{
\item{genome}{Character(1) specifying the genome build. Must be one of
\code{"hg19"}, \code{"hg38"}, \code{"mm9"}, or \code{"mm10"} (case-insensitive).
If \code{NULL}, no change is made and the current setting is reported.}
}
\value{
Invisibly returns the canonical genome string (e.g., \code{"hg38"})
if a new value is set; otherwise invisibly returns the currently set
genome (or \code{NULL} if unset).
}
\description{
This helper sets a global option used by \code{whereAreECDNAs} to determine
the default genome build (e.g., \code{"hg19"}, \code{"hg38"}, \code{"mm9"}, \code{"mm10"}).
}
\details{
If \code{genome} is \code{NULL}, the function leaves the current option unchanged and
reports the active genome, when one is set, along with the supported genomes.
}
\examples{
\dontrun{
  ## Query the current genome and supported builds
  addwhereAreGenomes()

  ## Set genome to hg38
  addwhereAreGenomes("hg38")
}

}
