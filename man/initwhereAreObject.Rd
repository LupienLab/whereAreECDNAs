% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CreateClass.R
\name{initwhereAreObject}
\alias{initwhereAreObject}
\title{Create a \code{whereAreECDNAs} object}
\usage{
initwhereAreObject(
  sample = NULL,
  nth = NULL,
  fragment = NULL,
  barcode = NULL,
  amplicon = NULL,
  nThreads = 1L,
  outputDirectory = NULL,
  genomeAnnotation = setGenomeAnnotation(),
  verbose = TRUE
)
}
\arguments{
\item{sample}{Character(1). Sample name.}

\item{nth}{Character(1). Identifier for a specific ecDNA amplicon or species.}

\item{fragment}{A \code{GRanges} storing fragments with at least a \code{barcode} metadata column (and optionally \code{count}).}

\item{barcode}{Character vector of high-quality cell barcodes for this sample.}

\item{amplicon}{A \code{GRanges} representing ecDNA amplicon intervals.}

\item{nThreads}{Integer(1) >= 1. Number of threads reserved for downstream steps.}

\item{outputDirectory}{Character(1). Output directory. If \code{NULL}, defaults to \code{paste0(sample, ".outputs")}.}

\item{genomeAnnotation}{Genome annotation object returned by \code{setGenomeAnnotation()}.
The value is validated with \code{.validGenomeAnnotation()}.}

\item{verbose}{Logical(1). Whether to print progress messages.}
}
\value{
A \code{whereAreECDNAs} S4 object.
}
\description{
Initializes a \code{whereAreECDNAs} S4 object containing sample metadata,
high-quality cell barcodes, fragments, ecDNA amplicons, genome annotation,
and output-directory information. The object is the central container for
downstream analysis steps, including pseudobulk fragment generation, peak
calling, weighted cell-by-peak matrix construction, ecDNA content
quantification, and ecDNA-positive cell prediction.
}
\examples{
\dontrun{
  object <- initwhereAreObject(
    sample = "sample",
    nth = "nth",
    fragment = fragment,
    barcode = barcode,
    amplicon = amplicon
  )
}
}
